# Week 2 — Express.js RESTful API


## Overview
This project implements a RESTful API for a `products` resource using Express.js covering CRUD operations, middleware (logger, auth, validation), error handling, filtering, pagination, search, and statistics.


## Setup
1. Ensure Node.js v18+ is installed.
2. Clone your GitHub Classroom repo (or copy these files into a folder).
3. Install dependencies:


```bash
npm install